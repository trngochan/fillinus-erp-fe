# VITE_API_URL — Backend API base URL
# Local dev: leave empty (uses Vite proxy → http://localhost:8080/api)
# Production: set to your deployed backend URL
VITE_API_URL=https://fillinus-erp.onrender.com/api
